// Marcadores de seção ("NN — LABEL") removidos a pedido — componente neutralizado.
// Mantido como no-op pra não quebrar os imports existentes.
export function SectionLabel(_props: { num: string; label: string; light?: boolean }) {
  void _props
  return null
}
